package com.server.domain.help.repository;

import com.server.domain.help.HelpImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HelpImageRepository extends JpaRepository<HelpImage, Long> {
}
