package com.server.domain.user.repository;

import com.server.domain.user.UserSubInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserSubInfoRepository extends JpaRepository<UserSubInfo, Long> {
}
